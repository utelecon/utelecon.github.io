---
import Ja from "./Ja.astro";
import En from "./En.astro";

export interface Author {
  affiliation: string;
  name?: string;
}

interface Props {
  lang: "ja" | "en";
  author: Author;
}

const { lang, author } = Astro.props;
---

{
  {
    ja: <Ja author={author} />,
    en: <En author={author} />,
  }[lang]
}
