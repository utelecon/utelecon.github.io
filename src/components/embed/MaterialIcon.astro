---
import {
  type Style,
  type ReactSVGAttributes,
  load,
} from "./utils/materialIcon";

type Props = {
  name: string;
  style?: Style;
  class?: string;
  svgProps?: ReactSVGAttributes;
};

const {
  class: className,
  name,
  style = "filled",
  svgProps = {
    height: undefined,
    width: undefined,
    fill: "currentColor",
  },
} = Astro.props;
---

<span class:list={[className, "material-icon"]}
  ><Fragment set:html={load({ name, style, props: svgProps })} /></span
>

<style>
  .material-icon {
    display: flex;
    align-items: center;
    justify-content: center;
  }
</style>
