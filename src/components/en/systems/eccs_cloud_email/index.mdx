import Configure from "./Configure.mdx";
import Login from "./Login.mdx";
import Access from "./Access.mdx";
import LoginSp from "./LoginSp.mdx";
import UseMail from "./UseMail.mdx";

{/**
  * @typedef {object} Props
  * @property {string} variant
  */}

- **Task: Set your preferred email address**
  <Configure
    support={props.variant === "oc"}
    detailsLink={props.important}
  />

- **Check: Log in**
  <Login />

- **Check: Make sure you can access UTokyo member-only pages**
  <Access support={props.variant === "oc"} />

- **Optional task: Log in on your smartphone** (for users of Gmail or other Google apps)
  <LoginSp support={props.variant === "oc"} />

- **Exercise: Use your email** (You can use your email address `xxxx@g.ecc.u-tokyo.ac.jp` in Gmail)
  <UseMail support={props.variant === "oc"} />